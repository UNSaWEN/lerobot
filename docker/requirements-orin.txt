# Runtime Python dependencies for LeRobot SO101 on NVIDIA Orin (lerobot v0.5.2).
#
# The following are provided by dustynv/lerobot base image — do NOT list them here:
#   torch, torchvision, opencv-python / opencv-python-headless
#
# torchcodec is intentionally omitted: aarch64 Jetson cannot satisfy the
# torchcodec>=0.11 / torch>=2.11 pairing. LeRobot falls back to PyAV decoding.

# --- core (lightweight) ---
draccus==0.10.0
huggingface-hub>=1.0.0,<2.0.0
requests>=2.32.0,<3.0.0
gymnasium>=1.1.1,<2.0.0
safetensors>=0.4.3,<1.0.0
packaging>=24.2,<26.0
termcolor>=2.4.0,<4.0.0
tqdm>=4.66.0,<5.0.0
einops>=0.8.0,<0.9.0
Pillow>=10.0.0,<13.0.0
numpy>=2.0.0,<2.3.0

# --- dataset (PyAV decoder, no torchcodec) ---
datasets>=4.7.0,<5.0.0
pandas>=2.0.0,<3.0.0
pyarrow>=21.0.0,<30.0.0
jsonlines>=4.0.0,<5.0.0
av>=15.0.0,<16.0.0

# --- hardware / feetech / phone ---
feetech-servo-sdk>=1.0.0,<2.0.0
pyserial>=3.5,<4.0
deepdiff>=7.0.1,<9.0.0
pynput>=1.7.8,<1.9.0
hebi-py>=2.8.0,<2.12.0
teleop>=0.1.0,<0.2.0
fastapi<1.0
scipy>=1.14.0,<2.0.0

# --- kinematics (Ubuntu 24.04 / JetPack 6 pins) ---
placo>=0.9.6,<0.9.16
cmeel-urdfdom>=4,<5
cmeel-tinyxml2<11

# --- visualization ---
rerun-sdk>=0.24.0,<0.34.0
